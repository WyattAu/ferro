apiVersion: v1
kind: Service
metadata:
  name: ferro
  namespace: ferro
  labels:
    app.kubernetes.io/name: ferro
    app.kubernetes.io/part-of: ferro
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ferro
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
